<h1>Cronjobs</h1>
For instance, let say you have to run a command 
 ```console
 $ uptime >> /tmp/system-report.txt
 ```
and redirect the results to a file called /tmp/system_report.txt. However, what if the command has to be run only at 9pm
everyday? This is when we use cron job. 

With a cron job, a user can specify any date, time, or frequency to schedule a task. Once defined, the system will execute the task at the set time without any human intervention. 
This functionlity is enabled by the cron daemon service that runs in the background. To schedule the job, run the command
 ```console
 $ crontab -e 
 ```
When you run the command, it will open the crontab in the VI editor
 ```console
 # For example, you can run a backup of all your user accounts 
 # at 5 a.m every week with:
 # 0 5 ** 1 tar -zcf /var/backups/home.tgz/home/
 #
 # m h   dom mon dow      command 
   0 21  *   *   *        uptime >> /tmp/system-report.txt
 ```
 The first five fields are used to specify the exact schedule to run the task. The sixth field is the command to run, which in our c ase is the command called uptime. 
 Do not use sudo with the crontab command, or the job will be scheduled for the root user. 

<h4> List all the jobs scheduled in Cron</h4>
```console
$ crontab -l
```
<h3>Meaning of the 5 fields </h3>
For example, you scheduling a task to run at 8:10 am on February 19th Monday. 
```console
10: minutes
8: hour
19: day
2: month
1: weekday
*: indictes any value
```
To run the job at 10 minutes past 8 on the 19th of all months, set the month value to a star.
```console
10: minutes
8: hour
19: day
*: month
*: weekday
```
To run the job at 10 minutes past 8 on all days of all month, set the day value to a star.
```console
10: minutes
8: hour
*: day
*: month
*: weekday
```
To run the job at 10 minutes past all hours on all day of all months, set the hour value to star.
```console
10: minutes
*: hour
*: day
*: month
*: weekday
```
To run the job every minute of every hour on all days of all months, set the minute value to a star. Your job will now run every minute.
```console
*: minutes
*: hour
*: day
*: month
*: weekday
```
Let say you want to run the job every two minutes? We may configure that using the step values with a slash. Star divided by two indicates that the the job should run every second. 
```console
*/2 : minute
*: hour
*: day
*: month
*: weekday
```
To run every day at 9pm
```console
00: minute
21: hour
*: day
*: month
*: weekday
```
<h3>Examples </h3>
February 19th, 08:10am, only if it's a Monday
```console
10: minute
8: hour
19: day
2: month
1: weekday
```
February 19th, 08:10 am, any weekday
```console
10: minute
8: hour
19: day
2: month
*: weekday
```
19th of every month at 08:10am, any weekday
```console
10: minute
8: hour
19: day
*: month
*: weekday
```
Every day of every month at 08:10am, any weekday
```console
10: minute
8: hour
*: day
*: month
*: weekday
```
Every day of every month at 10 mints past every hour or any weekday
```console
10: minute
*: hour
*: day
*: month
*: weekday
```
Every day of every month at every minute past every hour or any weekday
```console
*: minute
*: hour
*: day
*: month
*: weekday
```
Every day of every month at every other minute past every hour or any weekday. eg: 08:02, 08:04, 08:06... 09:02, 09:04, 09:06
```console
*/2 : minute
*: hour
*: day
*: month
*: weekday
```
Every day of every month at every other minute past every other hour or any weekday. eg: 08:02 08:04, 08:06... 10:02, 10:04, 10:06... 12:02, 12:04, 12:06
```console
*/2: minute
*/2: hour
*: day
*: month
*: weekday
```
<h3>Check to see the job was run successfully</h3>
Check the file where the output is being appended
For example:
```console
$ cat /tmp/system-report.txt

21:00:00 up 20:15, 1 user, load average: 0.47, 0.50, 0.52
```
Another way to check is to inspect the log file /var/log/syslog
```console
$ tail /var/log/syslog

Jul 22 21:00:01 caleton-lp10 CRON[1720]: (michael) CMD (uptime >> /tmp/system-report.txt)
```
Tail command in linux is a command-line utility used to display the last part (10 lines by default) of one or more files

